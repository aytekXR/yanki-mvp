# Session 4 — 2026-07-10 — e2e CI fix: all five jobs green (P4.4 done)

*Orchestrated per [resume-prompt.md](../resume-prompt.md) via the session-3
brief + its §9 post-close addendum. Gate check first: `deploy/.env` still has
no keys (P4.1 stays blocked), but the repo is on GitHub with the e2e job red —
so the addendum's directive ran: **fix the e2e job, prove all five CI jobs
green**. Executed as three small workflows (investigate: 3 agents — local
Docker repro/verify, action-major research, landmine audit; verify: 2 agents —
actionlint + adversarial diff review; docs-verify at close). Two commits
pushed; both CI runs green.*

## 1. Session summary

**Completed: the e2e CI job is fixed and the repo's CI story is closed —
all five jobs green, twice.** P4.4 flips to done (proven, not just authored).

- **The fix** (`6f7d64a`): the e2e job now runs `npm ci` + `npx playwright
  install --with-deps chromium` **before** booting the bind-mounting compose
  stack. Mechanism (confirmed by local repro in a scratch checkout, uid
  evidence captured): the web service bind-mounts `../frontend:/app` with
  anonymous volumes at `/app/node_modules` + `/app/.next`; when the host dir
  doesn't exist, dockerd creates the mountpoint **as root**, so the runner's
  later `npm ci` died `EACCES` (first-push run 29058049101). With the dir
  pre-created runner-owned, dockerd reuses it and ownership is preserved —
  verified locally end-to-end (npm ci green → boot → ownership still uid
  1000 → API/web health green → `POST /api/v1/analyses` → 202) before
  pushing. **Run 29059944092: 5/5 green; the Playwright spec executed for
  the first time anywhere — `1 passed (6.6s)`, not skipped.**
- **Action bumps** (`ec19e3e`): checkout v4→v7, setup-node v4→v6, setup-uv
  v5→v7, clearing GitHub's Node-20 deprecation annotations. Release notes
  were checked against our exact usage first (bare checkout + one
  `fetch-depth: 0`; setup-node without `packageManager` so v5+ auto-caching
  stays off; setup-uv used bare). setup-uv stops at v7: the v8 line dropped
  floating major tags, so `@v8` doesn't resolve and v7 already runs node24.
  **Run 29060093072: 5/5 green, annotations gone.** One benign new
  annotation: setup-uv v7 enables uv caching by default and the backend +
  contract jobs race to save the same cache key — the loser's save is a
  no-op ("Unable to reserve cache"), not a failure.
- **Decisions on audit findings** (a 6-risk landmine audit ran before the
  patch): keep `example.com` in the spec — discovery genuinely fetches the
  submitted URL even under DRY_RUN, and the alternatives (mocking discovery,
  or a stack-served URL that the SSRF guard would reject) change app behavior
  purely for CI. Documented instead (comment in ci.yml + new tech-debt #9).
  The `npm ci || npm install` fallback and the db's host-port-5432 mapping
  were left as-is (minimal diff; fallback recorded as tech-debt #16).
- **Scope discipline:** the brief's fallback task (ESLint CLI migration, now
  debt #10) was deliberately NOT picked up — the push branch was unblocked,
  so the e2e fix was the session task; #10 stays the key-free fallback.
- **Baseline:** no app code changed (CI YAML only), so `make test` was not
  re-run locally; the full backend/frontend/contract/e2e suites ran twice on
  the real runner instead — green.

**Why this way:** the session-3 addendum explicitly made the e2e fix session
4's first task; local repro-then-verify before pushing kept the repo's
"always deployable" promise (no push-and-pray iterations on CI).

## 2. Documentation updates

- [implementation-plan.md](../implementation-plan.md) — session-4 block in
  Current Priority; Readiness snapshot (30/32 ≈ 94%, readiness ~75%, CI leg
  of the Phase-5 gate met); P4.3 status note; P4.4 status → done with proof;
  tech-debt references renumbered (#5→#3, #6→#4, #9→#7).
- [tech-debt.md](../tech-debt.md) — items 2–3 **repaid and removed**; list
  renumbered (old→new map in the header); new #9 (e2e egress dependency) and
  #16 (`npm ci || npm install` fallback masks lockfile drift).
- [operator-actions.md](../operator-actions.md) — session-4 status block;
  item 2 now "CI fully green" with per-job results; item 3 downgraded to
  fully optional.
- [operator-expected.md](../operator-expected.md) — tick-list refreshed:
  item 1 (keys) is the only gating item; item 2 closed as fully green.
- [past-prompt.md](../past-prompt.md) — session-4 brief archived.
- [design.md](../design.md) — untouched: no architectural change (a CI-job
  step reorder and action version bumps are not architecture).
- README — untouched (no CI badge, clone URL already fixed post-session-3).

## 3. Architectural changes

None. No ADRs added.

## 4. Technical debt

**Repaid:** old items 2 (e2e CI job red) and 3 (Playwright spec never
executed) — both cleared by run 29059944092. **Added:** #9 (e2e job needs
runner egress to example.com — accepted, documented in ci.yml) and #16
(`npm ci || npm install` can mask lockfile drift — hygiene). **Renumbered:**
see the tech-debt.md header for the old→new map; live docs updated, archived
session logs keep the old numbers by design. Top remaining risks: deploy
scripts never run (#1, cleared by P4.2), real providers never called (#2,
cleared by P4.1), no rate limiting before a public URL (#3, planned P5.6).

## 5. Current state

- **Implemented + verified:** full PRD flow under DRY_RUN (Phases 0–3);
  P4.3/P4.4/P4.5/P4.6 done — **all five CI jobs proven green on a real
  runner, twice** (runs 29059944092, 29060093072). 64 backend + 20 frontend
  tests; browser e2e now guards every push.
- **Blockers:** entirely operator-gated — (1) real keys → P4.1 (then P4.2
  supervised deploy). The Phase-5 build gate now lacks only P4.1 + P4.2.
- **Assumptions:** GitHub-hosted runners keep egress open (tech-debt #9);
  ubuntu-latest hosted runners satisfy the node24 action majors (verified
  2026-07-10); nothing new otherwise.

## 6. Next session prompt

---

> **Yanki — Session 5 brief**
>
> Read `README.md`, `docs/session-rules.md`, `docs/implementation-plan.md`
> (Current Priority), `docs/tech-debt.md` (renumbered this session),
> `docs/operator-actions.md`, and `docs/sessions/2026-07-10-02.md`. The repo
> is a fully verified MVP: DRY_RUN flow proven locally AND **all five CI jobs
> green on GitHub** (browser e2e included). Phase 5 (P5.1–P5.11) is decomposed
> but **frozen** — its gate now lacks only P4.1 + P4.2.
>
> **First, check what the operator unblocked:**
> - If `deploy/.env` has real keys → **P4.1** (one real analysis, `DRY_RUN=0`;
>   record measured cost per analysis from the `cost_usd` columns vs provider
>   dashboards; compare against the plan margins in
>   `docs/00-first-mvp-draft.md`). Then **P4.2** becomes eligible — first
>   supervised deploy per its task card; never run `make deploy` unsupervised.
> - If no keys → the ONLY key-free task left is hygiene debt **#10**: migrate
>   `next lint` → ESLint CLI (`frontend/package.json` lint script + eslint
>   config; the frontend CI job's Lint step must stay green). Do NOT start
>   Phase 5 (gate unmet) and do NOT invent scope. If #10 is done too, close
>   with a short no-op session log restating what the operator must unblock.
>
> **Constraints:** never commit secrets (gitleaks in CI + pre-commit); never
> hand-edit `shared/contracts/openapi.json` or `frontend/lib/types.ts`
> (`make gen-types`); docs change in the same session as code; scope frozen
> per `docs/02-mvp.md` §4; end with the **eight** deliverables per
> `docs/session-rules.md`.
>
> **Warnings:** dev-host ports 5432/8140 taken — use
> `YANKI_DB_PORT=5434 YANKI_WEB_PORT=8240`; docker needs `sg docker -c "…"`;
> `deploy/*.sh` still UNTESTED; gitleaks pinned in two places (ci.yml +
> .pre-commit-config.yaml) — bump in lockstep; axe tests do NOT guard contrast
> (jsdom) — recompute ratios by hand if touching color tokens; tech-debt was
> RENUMBERED in session 4 (old→new map in its header) — archived session logs
> cite old numbers; the e2e CI job must keep its install-before-boot step
> order (see the comment in ci.yml) and never gain a host-side step that
> writes `frontend/.next`; setup-uv is capped at @v7 (v8 dropped floating
> tags); if using the Workflow tool, pass `args` as a real JSON object.
>
> **Acceptance:** the chosen task's acceptance criteria in
> `docs/implementation-plan.md` are met, statuses updated, debt updated, and
> a session log with a next-session prompt is written to `docs/sessions/`.

---

## 7. Docs inventory audit

All 24 repo `.md` files checked (23 from session 3 + this log;
dependency/build artifacts excluded): **all live, required, and correctly
placed** — README / SECURITY / CONTRIBUTING at root, PR template in
`.github/`, informative docs under `docs/`, archives under `docs/sessions/` +
`past-prompt.md` + the numbered `00-`/`01-` sources. **6 files updated this
session** (implementation-plan, tech-debt, operator-actions,
operator-expected, past-prompt, + this log); **0 moved, 0 deleted,
0 deprecated found.** Working tree verified clean of scratch artifacts before
commit.

## 8. Operator checklist refresh

Both files refreshed at close:
[operator-expected.md](../operator-expected.md) (item 2 closed green; item 1
is the single gating item; item 3 fully optional) and
[operator-actions.md](../operator-actions.md) (session-4 status + per-job CI
results). **Expected from the operator right now: real API keys (item 1) —
nothing else.** Everything after that (P4.2 deploy prerequisites, Phase-5
decisions) is listed but not yet due.
