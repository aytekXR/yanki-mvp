# Session 9 — 2026-07-10 (#07 today): P5.0 rate limit + P5.1 checker API, both live on prod

Operator prompt at session start: continue implementation using workflows,
log anything expected from the operator (chat + operator-expected.md), and
estimate sessions to "MVP ready". (Standing: workflows for code changes
only; eight session-end deliverables.)

## 1. Session summary

Two Phase-5 tasks landed, each via an implement → adversarial-verify
workflow, each deployed to prod and verified live:

1. **Session-start checks:** prod healthy (`DRY_RUN=0`, site 200, API
   serving), CI **5/5 green through session 8's push** (closing the
   "pushed without watching" caveat), and the prod DB showed **zero abuse**
   during the unprotected window — only our own 3 analyses, total spend
   $0.0162.
2. **P5.0 — rate limit on the LIVE `POST /api/v1/analyses`** (commit
   `31061c0`): migration 0002 (nullable `analyses.ip_hash`, salted sha256,
   raw IP never stored), reusable `services/rate_limit.py`, checks run
   BEFORE any row is created — 5/IP/rolling-hour + 100/day global rolling
   cap, 429 + precise `Retry-After`. The verifier added a regression test
   locking the no-row-on-429 property and flagged a latent 500 when a limit
   is set to 0; the orchestrator hardened that into a deliberate
   **kill-switch semantic** (limit 0 → 429 everything) with a test. Live
   acceptance on prod: one real submit ($0.0201, `ip_hash` persisted) + 4
   synthetic same-hash rows → next submit **429 `Retry-After: 3587`**;
   synthetic rows deleted (reset). Worst-case abuse now **≈$1.62/day**.
3. **P5.1 — checker submit + leads + 24h reuse** (commit `a8f0a06`):
   migration 0003 (nullable `kind`/`brand`/`category`/`lang` with server
   defaults — old prod rows verified backfilled `mvp`/`en` — plus
   append-only `checker_submissions`), `POST /api/v1/checker` → 202
   `{id, submission_id}` with normalized-triple (trim+casefold) 24h reuse
   and synthetic `checker://` url (ADR-19), `POST /api/v1/checker/leads`
   (each submission keeps its own email; re-lead overwrites only its own
   row), contract regenerated via `make gen-types` (additive-only), 13 new
   backend tests. **The verifier empirically caught a critical bug**: the
   live worker would have claimed `kind='checker'` rows, failed crawling
   `checker://…`, marked them `failed`, and thereby killed the reuse cache
   (reuse requires `done`). Fixed with a `claim_next` guard skipping
   checker rows + test — **P5.2 must remove it** (card updated, debt #19).
   Prod smoke at $0: cache hit returned the same analysis id for
   `" YANKISMOKE "` → `yankismoke`, lead attached (202), worker skipped
   checker rows (attempts=0); smoke rows deleted.
4. Suite at close: **82 backend passed + 2 PG-gated skips, 25 frontend**,
   ruff/mypy/tsc/eslint clean, gen-types drift-free. **CI 5/5 green on both
   `31061c0` and `a8f0a06`** (confirmed before close — the PG-gated queue
   tests and browser e2e ran green on the runner). Both deploys recorded
   last-good; co-tenants verified 200 after each.

**Session spend: $0.0201** (the P5.0 live-acceptance run). Cumulative live
spend to date ≈ **$0.056**.

## 2. Documentation updates

- `docs/implementation-plan.md` — session-9 entry; ➡️ next = P5.2→P5.3;
  readiness snapshot (Phase 5 **2/12**, plan 34/44 ≈ 77%, prod readiness
  ~98%); P5.0 + P5.1 cards → done; P5.2 card gains the guard-removal step.
- `docs/tech-debt.md` — **#2 repaid** (rewritten as the narrow XFF-trust
  posture note); new **#19** (P5.2 must remove the claim_next guard),
  **#20** (lead email regex), **#21** (checker endpoint unthrottled until
  P5.6, $0 exposure).
- `docs/design.md` — **ADR-19** (checker reuses `analyses` + synthetic url
  + append-only submissions; written in-workflow with the code).
- `docs/architecture.md` — P5.0 rate-limit section + 429 branch in the
  submit lifecycle (in-workflow).
- `deploy/.env.example` — 3 rate-limit vars (+kill-switch note) +
  `CHECKER_RESULT_CACHE_HOURS`.
- `docs/operator-expected.md` — refreshed (see §8).
- `docs/past-prompt.md` — session-9 pointer appended.
- This log.

## 3. Architectural changes

**ADR-19** (see design.md): the checker is `kind='checker'` rows in the
existing `analyses` table (nullable-columns-only migration, synthetic
`checker://` url satisfies the untouched NOT NULL, append-only
`checker_submissions` carries per-visit demand + leads). No other
architecture changed; P5.0 is a route-level guard + one nullable column.

## 4. Technical debt

- **Repaid:** #2 (unmetered live endpoint) — the session-8 ACTIVE risk.
- **Added:** #19 (temporary claim_next checker guard — removal is a P5.2
  acceptance step), #20 (minimal email regex), #21 (checker route
  unthrottled until P5.6; $0 today because of #19).
- #1 unchanged (KYC-cost persistence + adapter contract tests).

## 5. Current state

- **Implemented:** full MVP (32/32) LIVE at https://yanki.beyondkaira.com,
  live providers, measured $0.0162/analysis, **now rate-limited** (P5.0).
  Checker API surface live but inert (P5.1; runs stay queued at $0 until
  P5.2). Phase 5: **2/12**.
- **Incomplete:** P5.2–P5.11 (10 tasks). Next: **P5.2** (pipeline branch +
  fixed 12-prompt set + cache upsert; removes the worker guard), then P5.3.
- **Blockers:** none for P5.2–P5.6. Operator-gated later: P5.7 (Gemini +
  Perplexity keys), P5.11 (Turkish sign-off, abuse thresholds, email-gate
  strength, free-raw-answer choice), brandkit v2 (parked).
- **Assumptions this session:** (a) "continue implementation" + the
  session-8 handoff authorized the two supervised prod deploys (both
  followed the proven `make deploy` path with co-tenant checks); (b) the
  429 live test was designed to spend once (~$0.02) rather than 5×, using
  DB-injected synthetic rows, deleted after; (c) marking one smoke checker
  row `done` in the prod DB (then deleting it) was acceptable to prove the
  reuse path live at $0.

## 6. Next-session prompt

> **You are the founder-orchestrator for Yanki** (`/home/aytek/repo/yanki-mvp`,
> brief: `docs/resume-prompt.md`). Read `docs/session-rules.md`,
> `docs/implementation-plan.md` (Current Priority + readiness snapshot),
> `docs/tech-debt.md`, `docs/operator-expected.md`, and this log first.
>
> **State:** MVP 32/32 LIVE at https://yanki.beyondkaira.com
> (live-providers, $0.0162/analysis measured, **rate-limited**: 5/IP/hour +
> 100/day, limit 0 = kill-switch). Phase 5 is 2/12: P5.0 (31061c0) + P5.1
> (a8f0a06) deployed and live-verified. Checker rows currently sit `queued`
> forever by design (claim_next skips `kind='checker'` — debt #19). CI was
> 5/5 green on both commits at session-9 close (only the docs-close push
> was unwatched).
>
> **Priority: P5.2 — checker pipeline branch** (card in
> `docs/implementation-plan.md`): seed-string discovery branch for
> `kind='checker'` (no crawl), fixed VERSION-stamped EN 12-prompt set via
> `checker_prompts.generate(kyc, lang)`, `execute._write_cache` → upsert
> (repays debt #6), **remove the claim_next checker guard + replace its
> skip test** (debt #19 — the card lists this explicitly). Then P5.3
> (read-time presence map + competitors) if room. All DRY_RUN-testable, $0.
>
> **Constraints:** repo always runnable (`make dev`/`make test`); workflows
> (implement → adversarial-verify) for code changes, never docs-only; never
> hand-edit generated files (`make gen-types`); docs in the same session as
> code; scope frozen per `docs/02-mvp.md` §4 + the Phase-5 cards; eight
> session-end deliverables per `docs/session-rules.md`; Workflow `args`
> must be real JSON.
>
> **Warnings:** PROD IS LIVE ON THIS BOX — project `yanki-prod`, loopback
> health 8142/8143 (health path is `/healthz`), attached to
> `pulse-prod_default` (never take pulse-prod down while attached; never
> restart the shared Caddy; never append the yanki Caddy block twice —
> debt #7). Deploys via `sg docker -c "make deploy"` (api runs alembic on
> boot; alembic can't run on SQLite locally — 0001 uses JSONB). A live MVP
> analysis costs ~$0.016-0.02 — the P5.2 acceptance is fully DRY_RUN/$0;
> if you must submit on prod, note the spend. After P5.2, checker rows
> WILL run on live providers when submitted — the endpoint has no rate
> limit until P5.6 (debt #21): consider sequencing P5.6 immediately after
> P5.2 if P5.3 doesn't fit, and don't leave prod deployed mid-session with
> checker runnable but unthrottled without flagging it in
> operator-expected. Dev ports: 5432/8140 taken (`YANKI_DB_PORT=5434
> YANKI_WEB_PORT=8240`); worker logs one cosmetic UndefinedTable at first
> boot (debt #16); analysis terminal status is `done`; `geo_score` nested
> under `result`.
>
> **Acceptance:** P5.2's card criteria (12 prompts × 4 mock engines = 48
> responses, no crawl, no divide-by-zero, byte-stable prompt set, upsert
> race test, guard removed) + `make test` green + CI green; statuses +
> debt updated (repay #6/#19); session log with next-session prompt;
> operator checklist refreshed.

## 7. Docs inventory audit

All repo `.md` files checked (27 from session 8 + this log = 28; brandkit's
five `.md` files remain parked package components by operator decision;
dependency/build artifacts excluded): **all live, required, and correctly
placed.** 7 updated this session (implementation-plan, tech-debt, design,
architecture, operator-expected, past-prompt, + this log); 0 moved,
0 deleted, 0 deprecated found.

## 8. Operator checklist refresh

[operator-expected.md](../operator-expected.md) fully refreshed. **Expected
from the operator right now: nothing urgent, nothing blocking.** Two
recommended items stand (both carried from session 8, now lower stakes):

1. **Item 1 (~5 min, defense-in-depth): provider-console spend caps** —
   code-side limits now bound abuse at ≈$1.62/day, so this protects only
   against provider-side surprises. Kill-switch alternative documented
   (`ANALYSES_DAILY_CAP=0` + redeploy).
2. **Item 2 (2 min): eyeball the KYC card** on the live result page.

Coming due later: item 12 (Gemini/Perplexity keys, ~2–3 sessions out at
P5.7), item 13 (checker go-live product decisions, at P5.11).
