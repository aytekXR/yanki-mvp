# Session 7 — 2026-07-10 (#05 today): P4.2 — the first real deploy; the site is LIVE

Operator directive at session start: **"Let's deploy website. Using
workflows, continue implementation"** + log anything expected from the
operator both in-chat and in `operator-expected.md`.

## 1. Session summary

**P4.2 is done — https://yanki.beyondkaira.com is live, and the MVP plan
(Phases 0–4) is 32/32 = 100%.**

What happened, in order:

1. **Gate checks** (per the session-6 brief): `deploy/.env` had real keys ✅,
   placeholder `POSTGRES_PASSWORD` ❌ (fixed below), pulse-prod up ✅, ports
   8142/8143 free ✅, DNS resolving ✅, no yanki block in the shared
   Caddyfile yet ✅ (expected).
2. **Pre-flight verification workflow** (operator asked for workflows; this
   was the right use — the deploy path had never run): 4 parallel reviewer
   agents (scripts / compose-topology / caddy / frontend-image), 36 checks
   passed, **0 blockers**, 18 warnings. Highest-value outputs: the
   concatenated Caddyfile validated clean *inside the live Caddy container*
   (host-side validation can't resolve its env placeholders); no
   alias/volume/port collisions on the live host; `docker compose config`
   proves the `environment:` DATABASE_URL overrides the env_file's localhost
   one; the first-deploy failure path can't touch co-tenants; appending the
   Caddy block twice fails validation (non-idempotent — now documented).
3. **Operator item 4 executed under the deploy directive:** generated a
   32-char random `POSTGRES_PASSWORD` into `deploy/.env` (gitignored; fresh
   prod volume so it took effect), and pinned `YANKI_PROD_WEB_PORT=8142` /
   `YANKI_PROD_API_PORT=8143` explicitly (they previously worked only because
   the compose default and the deploy.sh default happened to agree).
4. **First `make deploy` failed usefully** — the never-tested prod web image
   build broke: `ENV NODE_ENV=production` makes `npm ci` omit
   devDependencies, and `next build` needs the `typescript` devDep to
   transpile `next.config.ts`. Fix: `npm ci --include=dev || npm install
   --include=dev` (commit `3a84943`). Nothing had started (failure was at
   build, before `compose up`).
5. **Second `make deploy` green end-to-end:** images `yanki-api:3a84943` /
   `yanki-web:3a84943` built, fresh `yanki-prod` stack up, alembic migrated
   on boot, `/healthz` passed, `.last-good = 3a84943` recorded. A mock
   analysis was run against the prod API and completed (status `done`,
   `geo_score 0.6`, 24/40 footprint); the frontend analysis page rendered
   (HTTP 200 on `127.0.0.1:8142`).
6. **Publish through the shared Caddy** (the one co-tenant-risky step):
   baseline co-tenant checks recorded (pulse 200 / apex 200 / www 301 /
   ams 200) → live Caddyfile backed up → yanki block appended to
   `~/repo/ams-pulse/deploy/config/Caddyfile.prod` → `caddy validate`
   in-container ("Valid configuration") → `caddy reload` (**not** restart) →
   **all four co-tenants matched baseline exactly** → TLS for
   `yanki.beyondkaira.com` issued immediately; site serves 200 with valid
   TLS; `/healthz` and `/api/v1/analyses/{id}` route correctly through the
   path-routing block.
7. **`make rollback` exercised** (P4.2 acceptance): same-SHA rollback ran
   clean and healthy; site still live after.
8. **Deliberate decision: prod runs `DRY_RUN=1` (mock) for now.** Real keys
   are in the env, but there is no rate limiting yet (tech-debt #2) — a
   public URL with live providers is unmetered spend. Flipping to live is
   operator-expected item 1.

Worker note for the record: at first boot the worker logged one
`UndefinedTable` traceback (it polls before the api container's migration
finishes), then recovered; RestartCount stayed 0. Filed as tech-debt #16
(cosmetic).

## 2. Documentation updates (same session as the code)

- `docs/implementation-plan.md` — session-7 entry; P4.2 → ✅ done with
  evidence; readiness snapshot rewritten (32/32; ~95%; Phase-5 gate OPEN);
  tech-debt references renumbered; stale UNTESTED notes cleared.
- `docs/tech-debt.md` — old #1 repaid/removed; renumbered (map in header);
  old #8 rewritten as #7 (wiring proven; manual non-idempotent publish +
  two-way coupling remain); new #16 (worker boot race), #17 (rollback's
  pruned-image branch unproven + `git checkout` hazard), #18 (prod web image
  ships devDependencies).
- `docs/operator-expected.md` — fully refreshed (see §8).
- `docs/architecture.md` — deploy section: UNTESTED marker → exercised.
- `README.md` — deploy section: site live; all three one-time prerequisites
  marked done; duplicate-append warning added.
- `deploy/deploy.sh` / `deploy/rollback.sh` — header comments updated
  (UNTESTED → first exercised; rollback's pruned-image branch still unproven).
- `docs/past-prompt.md` — session-7 brief archived.

## 3. Architectural changes

None. The deploy executed the architecture exactly as designed (ADRs
unchanged); the Dockerfile.prod change is a build bug fix, not a design
change. The one design-relevant confirmation: the shared-Caddy
alias-routing pattern works as specified on the first try.

## 4. Technical debt

Repaid: old #1 (deploy/rollback scripts never run). Added: #16, #17, #18
(see §2). Rewritten: #7. Note the compose/deploy port pinning removed a
latent desync (documented inside operator-expected item 4 / .env comments).

## 5. Current state

- **Implemented:** the full MVP — 32/32 tasks, Phases 0–4. The product runs
  live at https://yanki.beyondkaira.com (mock pipeline mode), CI 5/5 green,
  deploy/rollback proven, cost model validated ($0.0132/analysis).
- **Incomplete:** Phase 5 (public checker) — 0/11, gate now open. P4.1's
  OpenAI cost leg (operator billing). Prod live-provider mode (operator
  decision).
- **Blockers:** none for P5.1–P5.6. Operator-gated: OpenAI leg (item 2),
  DRY_RUN flip (item 1), Gemini/Perplexity keys (item 11, only at P5.7+).
- **Assumptions made this session:** (a) "Let's deploy" authorized the
  supervised-deploy steps including the shared-Caddy edit and generating the
  prod DB password — both logged prominently to the operator; (b) mock mode
  is the right prod default until rate limiting exists; (c) same-SHA
  rollback satisfies P4.2's rollback acceptance (the pruned-image branch is
  explicitly debt #17).

## 6. Next-session prompt

> **You are the founder-orchestrator for Yanki** (`/home/aytek/repo/yanki-mvp`,
> brief: `docs/resume-prompt.md`). Read `docs/session-rules.md`,
> `docs/implementation-plan.md` (Current Priority + readiness snapshot),
> `docs/tech-debt.md`, `docs/operator-expected.md`, and this log first.
>
> **State:** MVP complete (32/32) and LIVE at https://yanki.beyondkaira.com
> (prod stack `yanki-prod` on the same VPS, DRY_RUN=1 mock mode, TLS via the
> shared pulse-prod Caddy). CI 5/5 green as of run 29062634057 + local-only
> commits since (push happens at session close per operator instruction not
> to block on CI watches). Cost model validated live ($0.0132/analysis,
> Anthropic leg).
>
> **Priority:** the Phase-5 build gate is OPEN — start **P5.1** (checker data
> model + migrations; see its card in `docs/implementation-plan.md`) unless
> the operator redirects. Before building, do the standing gate checks:
> (1) if the operator added OpenAI credits (operator item 2), first run the
> one cheap OpenAI re-run (~$0.02) to record that cost leg and close the
> P4.1 residual — use a SCRATCHPAD compose override for DRY_RUN/PANEL_ENGINES,
> never edit `deploy/.env`; (2) if the operator ticked item 1 (DRY_RUN=0 on
> prod), verify rate-limiting expectations with them before leaving it on.
>
> **Constraints:** repo always runnable (`make dev`/`make test`); never
> hand-edit generated files (`make gen-types`); docs in the same session as
> code; scope frozen per `docs/02-mvp.md` §4; eight session-end deliverables
> per `docs/session-rules.md`; no workflows for docs-only changes; Workflow
> `args` must be real JSON.
>
> **Warnings:** dev ports — 5432/8140 taken (`YANKI_DB_PORT=5434
> YANKI_WEB_PORT=8240`); docker via `sg docker -c`; **prod is live on this
> box** — project `yanki-prod`, loopback health ports 8142/8143, its web/api
> are attached to `pulse-prod_default` (never `docker network rm` /
> `pulse-prod down` while attached — two-way coupling, debt #7); the shared
> Caddy is live TLS for four other sites — validate-in-container before any
> reload, NEVER restart it, never append the yanki block twice (debt #7);
> the yanki site block now lives in the OPERATOR'S ams-pulse repo
> (uncommitted — operator item 3); worker logs one UndefinedTable at first
> boot (debt #16, cosmetic); analysis terminal status string is `done`;
> gitleaks pinned in two places; e2e CI install-before-boot order is
> load-bearing.
>
> **Acceptance:** chosen task's criteria in `docs/implementation-plan.md`
> met; statuses + debt updated; session log with next-session prompt in
> `docs/sessions/`; operator checklist refreshed.

## 7. Docs inventory audit

All repo `.md` files checked (25 from session 6 + this log = 26; brandkit's
five `.md` files remain parked package components by operator decision;
dependency/build artifacts excluded): **all live, required, and correctly
placed.** 7 updated this session (implementation-plan, tech-debt,
operator-expected, architecture, README, past-prompt, + this log); 0 moved,
0 deleted, 0 deprecated found. `deploy/caddy/yanki.beyondkaira.com.caddy`
is now documentation-plus-source-of-truth-copy of a block that also lives in
the ams-pulse repo — noted in tech-debt #7 as a manual two-file sync.

## 8. Operator checklist refresh

[operator-expected.md](../operator-expected.md) fully refreshed. **Expected
from the operator right now, in priority order:**

1. **Item 1 — decide prod run mode:** stays mock (`DRY_RUN=1`, current,
   $0) or flips live (`DRY_RUN=0` + redeploy) — recommendation: flip only
   for supervised demos until P5.6 rate limiting lands.
2. **Item 2 — OpenAI billing/credits** (key returns `insufficient_quota`) —
   unblocks the last P4.1 residual (~$0.02 re-run).
3. **Item 3 — commit the +35-line Caddyfile edit in your ams-pulse repo**
   (it's live but uncommitted there).

FYI-only (done for them): real `POSTGRES_PASSWORD` generated into
`deploy/.env` (worth copying to a password manager). Items 11–13 (Phase-5
keys/decisions, brandkit v2) are parked, not due.
