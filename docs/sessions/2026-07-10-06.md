# Session 8 — 2026-07-10 (#06 today): live providers on prod, KYC card, OpenAI cost leg

Operator directives at session start: **run mode → live-providers; KYC is
very important — show it on the result page; OpenAI is accessible now;
Caddyfile pushed** (+ standing: workflows for code changes only, log
operator asks in chat AND operator-expected.md, prepare the next-session
prompt).

## 1. Session summary

All four directives executed and verified:

1. **KYC card (workflow: implement → verify).** The result page's raw
   `<pre>{JSON.stringify(kyc)}</pre>` dump became a structured, prominent
   profile card (`frontend/components/KycCard.tsx`): company name large,
   description as prose, industry/locations in a semantic `<dl>`,
   aliases/keywords/products/services/competitors as labeled chip lists —
   all house tokens, no new color pairs (axe can't check contrast under
   jsdom). The independent verifier agent caught a real crash risk the
   implementer missed (KYC is unvalidated free-form JSON — *missing* fields
   would have thrown where *empty* fields were handled), fixed it with
   coercion guards, and added a regression test. 5 new tests (rendering +
   partial + bare-profile + 2 axe); tsc / eslint / vitest 25/25 /
   `next build` all green, twice (implementer + verifier). Commit `d75c852`.
2. **OpenAI quota pre-verified before touching prod** — one direct
   `gpt-5-nano` call. Learning recorded: a 16-token `max_completion_tokens`
   probe fails ("output limit reached") because reasoning tokens bill as
   output even at `reasoning_effort="minimal"`; 256 tokens → "OK" in 18
   total tokens. Our provider's `MAX_TOKENS = 1024` has ample headroom.
3. **Prod flipped to live** (`DRY_RUN=1→0` in `deploy/.env`, an operator
   directive, so the standing "never edit the operator's env" rule was
   explicitly lifted for this line) and redeployed — one deploy shipped both
   the KYC card and the mode flip (last-good `d75c852`). Container env
   verified (`DRY_RUN=0`), co-tenants re-checked (pulse/apex/ams 200,
   yanki 200).
4. **First full live panel ON PROD via the public URL**
   (`https://www.anthropic.com`, analysis
   `17164747-a6a7-40ab-bc3b-d4d4d6e9ee62`, ~105s): real KYC (company
   Anthropic, correct industry/products/competitors), `geo_score 0.225`,
   9/40 footprint, 40 responses = 10 live Haiku 4.5 + 10 live `gpt-5-nano`
   + 20 stubs. **Cost from the prod DB: Anthropic $0.0135 + OpenAI $0.0026
   = $0.0162/analysis ≈ 1% of the $49 plan** — the P4.1 OpenAI residual is
   CLOSED (measured, not estimated).
5. **Consequence handled as a plan change (see §3/§4):** going live before
   rate limiting inverts the plan's original assumption. P5.6 only guards
   the future `/api/v1/checker` — so a new **P5.0** card (minimal per-IP
   rate limit + daily cap on `POST /api/v1/analyses`, complexity S) is now
   the first Phase-5 task, and the operator was asked to set provider
   console spend caps today (operator item 1).
6. Confirmed the operator's ams-pulse Caddyfile commit (`d538631`, clean
   tree) — operator item ticked.

## 2. Documentation updates

- `docs/implementation-plan.md` — session-8 entry; readiness snapshot
  (32/32 all-residuals-closed; Phase 5 now 0/12; ~97%); P5.0 card added;
  sequencing table + build order updated.
- `docs/tech-debt.md` — #1 rewritten (both adapters proven; residuals =
  KYC-cost persistence + contract tests); #2 marked ACTIVE with the P5.0
  mitigation plan.
- `docs/operator-expected.md` — fully refreshed (items 1–2 due: console
  spend caps, eyeball the KYC card).
- `docs/past-prompt.md` — session-8 pointer appended.
- This log.

## 3. Architectural changes

None structurally. One **operational posture change**: production now runs
live providers (real spend per analysis) — recorded here, in tech-debt #2,
and in the readiness snapshot rather than as an ADR (no design changed; an
assumption did).

## 4. Technical debt

- #1 largely repaid (OpenAI leg measured live); remainder re-scoped.
- #2 escalated from "accepted while private" to "ACTIVE on a public URL" —
  mitigation P5.0 (next session), console caps (operator), DRY_RUN=1
  escape hatch documented.
- No new items.

## 5. Current state

- **Implemented:** full MVP (32/32) LIVE at https://yanki.beyondkaira.com in
  **live-provider mode**; structured KYC card on results; measured
  full-panel cost $0.0162/analysis; CI 5/5 green; deploy/rollback proven.
- **Incomplete:** Phase 5, 0/12 — P5.0 (urgent rate limit) then P5.1+.
- **Blockers:** none for P5.0–P5.6. Operator-gated later: P5.7 keys, P5.11
  go-live decisions, brandkit v2.
- **Assumptions this session:** (a) "run mode: live-providers" authorized
  editing `DRY_RUN` in `deploy/.env` (narrow lift of the never-edit rule);
  (b) "show this file on result file" = render the KYC profile properly on
  the analysis result page (it was already technically present as raw
  JSON); (c) leaving PANEL_ENGINES with gemini/perplexity stubs in the live
  panel is intended until P5.7.

## 6. Next-session prompt

> **You are the founder-orchestrator for Yanki** (`/home/aytek/repo/yanki-mvp`,
> brief: `docs/resume-prompt.md`). Read `docs/session-rules.md`,
> `docs/implementation-plan.md` (Current Priority + readiness snapshot),
> `docs/tech-debt.md`, `docs/operator-expected.md`, and this log first.
>
> **State:** MVP 32/32, LIVE at https://yanki.beyondkaira.com in
> **live-provider mode** (`DRY_RUN=0`; real Anthropic + OpenAI, measured
> $0.0162/analysis; gemini/perplexity stubs). KYC renders as a structured
> card. CI 5/5 green as of session 6's push; sessions 7–8 commits pushed
> without watching CI (standing operator instruction). Phase 5 is 0/12.
>
> **Priority: P5.0 — minimal per-IP rate limit + daily cap on the LIVE
> `POST /api/v1/analyses`** (card in `docs/implementation-plan.md`; S-sized;
> repays the active half of tech-debt #2). The endpoint is public with real
> keys TODAY — this outranks P5.1. Then P5.1 (checker schema + submit) if
> the session has room. Check first whether the operator set console spend
> caps (operator item 1) — if not, restate it; it does not block P5.0.
>
> **Constraints:** repo always runnable (`make dev`/`make test`); never
> hand-edit generated files (`make gen-types` for OpenAPI/types); docs in
> the same session as code; scope frozen per `docs/02-mvp.md` §4 (P5.0 is
> in-plan — added session 8); eight session-end deliverables per
> `docs/session-rules.md`; workflows for code changes, never for docs-only;
> Workflow `args` must be real JSON.
>
> **Warnings:** PROD IS LIVE ON THIS BOX — project `yanki-prod`, loopback
> health ports 8142/8143, attached to `pulse-prod_default` (never take
> pulse-prod down while attached; never restart the shared Caddy; never
> append the yanki Caddy block twice — debt #7). P5.0's migration lands via
> `make deploy` (api container runs `alembic upgrade head` on boot). A live
> analysis costs real money (~$0.0162) — use it sparingly for the 429
> acceptance test and note the spend. Dev ports: 5432/8140 taken
> (`YANKI_DB_PORT=5434 YANKI_WEB_PORT=8240`); docker via `sg docker -c`;
> worker logs one cosmetic UndefinedTable at first boot (debt #16);
> analysis terminal status is `done`; `geo_score` is nested under `result`.
>
> **Acceptance:** P5.0's card criteria met (429 over limit verified on
> prod, then reset; CI green; migration clean via redeploy); statuses +
> debt updated; session log with next-session prompt in `docs/sessions/`;
> operator checklist refreshed.

## 7. Docs inventory audit

All repo `.md` files checked (26 from session 7 + this log = 27; brandkit's
five `.md` files remain parked package components by operator decision;
dependency/build artifacts excluded): **all live, required, and correctly
placed.** 5 updated this session (implementation-plan, tech-debt,
operator-expected, past-prompt, + this log); 0 moved, 0 deleted,
0 deprecated found.

## 8. Operator checklist refresh

[operator-expected.md](../operator-expected.md) fully refreshed. **Expected
from the operator right now:**

1. **Item 1 (urgent, ~5 min): set hard spend caps in the Anthropic and
   OpenAI consoles** — prod is live with real keys and no rate limiting
   until P5.0 lands next session. Escape hatch: `DRY_RUN=1` + redeploy.
2. **Item 2 (optional, 2 min): eyeball the KYC card** on the live result
   page and say if you want content/order changed.

Everything else is done or parked (items 12–14: Phase-5 keys, checker
decisions, brandkit v2).
