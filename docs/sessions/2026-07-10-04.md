# Session 6 — 2026-07-10 — cheapest models + first LIVE run (P4.1 done)

*Orchestrated per [resume-prompt.md](../resume-prompt.md). Two operator
directives arrived mid-session: **"use the cheapest models from Anthropic
and OpenAI"**, then **"just added the API keys"** — which turned the model
switch into the prelude for P4.1, executed immediately. No workflows were
used: the live end-to-end run is the strongest verification this change can
get, and the operator has standing instructions against workflows for small/
doc work.*

## 1. Session summary

**Completed: the cheapest-model switch and P4.1 — the first live run of the
pipeline, with the cost model validated.**

- **Model directive.** Cheapest current models, verified against official
  sources (the claude-api reference for Anthropic; OpenAI's pricing +
  deprecations pages via web — never from memory):
  - **Anthropic: no change needed** — already on `claude-haiku-4-5-20251001`
    ($1.00/$5.00 per MTok), the cheapest current Anthropic model (Haiku 3.5
    retired, Haiku 3 deprecated).
  - **OpenAI: `gpt-4o-mini` ($0.15/$0.60) → `gpt-5-nano` ($0.05/$0.40)** —
    cheapest available (gpt-4.1-nano is $0.10/$0.40; the newer gpt-5.4-nano
    is $0.20/$1.25; gpt-5-nano is alive per the deprecations page).
    GPT-5-family quirks handled: `max_completion_tokens` (rejects
    `max_tokens`) and `reasoning_effort="minimal"` (reasoning tokens bill as
    output; minimal keeps that ~0). Cost math unchanged — usage-based.
- **P4.1 execution** (keys landed mid-session): stack booted live via a
  scratchpad compose override (`DRY_RUN=0` — the operator's `deploy/.env`
  untouched), ports 5434/8240 per host quirks.
  - **Attempt 1 failed usefully:** Anthropic calls returned 200 (KYC + first
    panel call), then OpenAI returned **`429 insufficient_quota`** on every
    retry — the operator's OpenAI key has no usable billing/credits. Logged
    as operator item **1b**.
  - **Attempt 2 succeeded** with a live-Anthropic panel
    (`PANEL_ENGINES=anthropic,gemini,perplexity`; stubs free): submitted
    `https://www.anthropic.com` → **done in ~40s**. Real KYC profile
    (Anthropic PBC, correct industry/keywords/products), `geo_score=0.2`
    (6/30 — the two stubs dilute by design), 30 responses.
  - **Measured cost: $0.0132/analysis** for the 10 real Haiku panel
    responses (~$0.0013 each, from `responses.cost_usd`). Whole-session
    spend incl. the failed attempt: < $0.05.
  - **Margin check (NFR-1, [00-first-mvp-draft.md](../00-first-mvp-draft.md)):**
    bar is API cost < 35% of plan price; measured ⇒ daily full-panel ≈
    $0.45/mo/customer ≈ **1% of the $49 plan** — ~35× headroom, no
    repricing needed. The OpenAI leg adds ~$0.002/analysis (est.) once
    quota exists.
  - Stack torn down cleanly after the run; the temporary override lives only
    in the session scratchpad.
- **Verification:** full suite green after the provider change (64 backend +
  20 frontend tests; ruff + mypy clean) — plus the live run itself.
- **Also this session (earlier turns):** deploy retargeted to
  `yanki.beyondkaira.com` on this VPS (see session-5 log §9 addendum) and
  brandkit v2 parked at the operator's direction (operator-expected item 10).

**Why this way:** the cheapest-model choice is the operator's explicit call
(cost floor for the Week-1 finance exercise); attempting P4.1 immediately
after keys landed follows the standing session brief ("if keys are present →
P4.1"); completing the Anthropic leg rather than blocking on OpenAI billing
preserves momentum while being honest about the residual.

## 2. Documentation updates

- [implementation-plan.md](../implementation-plan.md) — session-6 block;
  P4.1 → done with numbers (+ residuals); P2.2 card model name; readiness
  snapshot: **31/32 ≈ 97%, readiness ~85%**; on-track bullet records the
  operator's model directive.
- [operator-expected.md](../operator-expected.md) — item 1 ✅ with the
  measured numbers; **new item 1b: OpenAI quota**; P4.2 section retitled
  "Do next" (last MVP task).
- [tech-debt.md](../tech-debt.md) — #2 rewritten: Anthropic leg repaid,
  OpenAI leg pending quota, KYC-cost-not-persisted gap recorded.
- [past-prompt.md](../past-prompt.md) — session-6 entry appended.
- design.md untouched (model IDs are config-level, not architecture; no ADR).

## 3. Architectural changes

None. (Provider model/params changed inside the existing adapter seam.)

## 4. Technical debt

**Partially repaid:** #2 — the Anthropic adapter and the `cost_usd`
plumbing are live-proven. **Recorded residuals in #2:** OpenAI adapter
unproven (quota), KYC call cost not persisted, no adapter contract tests.
Top remaining: deploy scripts never run (#1, cleared by P4.2), rate limiting
before public URL (#3, planned P5.6).

## 5. Current state

- **Implemented + verified:** full PRD flow **live** (real Claude Haiku 4.5)
  and under DRY_RUN; CI 5/5 green; 64+20 tests; deploy configs reconciled
  with the real VPS topology (session 5 post-close).
- **Incomplete:** P4.2 (supervised deploy) — the last MVP task. Residuals:
  OpenAI cost leg (operator quota), KYC cost persistence (debt #2).
- **Blockers:** all operator-side: OpenAI billing (item 1b), shared-Caddy
  edit (item 6), supervised `make deploy` (item 7).
- **Assumptions:** gpt-5-nano's `max_completion_tokens` +
  `reasoning_effort="minimal"` params are correct per official docs but not
  yet proven live (the 429s prove auth/endpoint/SDK plumbing); pricing
  verified 2026-07-10.

## 6. Next session prompt

---

> **Yanki — Session 7 brief**
>
> Read `README.md`, `docs/session-rules.md`, `docs/implementation-plan.md`
> (Current Priority), `docs/tech-debt.md`, `docs/operator-expected.md`, and
> `docs/sessions/2026-07-10-04.md`. State: MVP 31/32 ≈ 97%; readiness ~85%;
> CI 5/5 green; **pipeline proven live** (P4.1 done, $0.0132/analysis
> Anthropic leg); deploy retargeted to **yanki.beyondkaira.com on this VPS**
> (P4.2 card has verified topology facts + co-tenant constraints).
>
> **Priorities, in order:**
> 1. **If the operator fixed OpenAI billing** (item 1b): one cheap re-run
>    with the full panel (`DRY_RUN=0`, override in scratchpad — never edit
>    `deploy/.env`), record the OpenAI cost leg in P4.1's card (~$0.02).
>    This also live-proves gpt-5-nano's `max_completion_tokens` +
>    `reasoning_effort` params.
> 2. **P4.2 — the supervised first deploy** (only WITH the operator, never
>    unsupervised): operator adds the Caddy block from
>    `deploy/caddy/yanki.beyondkaira.com.caddy` to the ams-pulse Caddyfile
>    (validate + RELOAD, never restart), then supervise `make deploy`;
>    acceptance includes every pre-existing site still serving before AND
>    after. Pulse-prod must be up first (external network).
> 3. If neither is unblocked: nothing to build — no-op close restating
>    items 1b/6/7. Do NOT start Phase 5 (gate lacks only P4.2); do NOT
>    touch `brandkit/` (operator item 10).
>
> **Constraints:** never commit secrets; never hand-edit generated contract
> files (`make gen-types`); docs in the same session as code; scope frozen
> per `docs/02-mvp.md` §4; eight session-end deliverables per
> `docs/session-rules.md`; no workflows for docs-only changes (operator
> instruction).
>
> **Warnings:** ports 5432/8140 taken (use `YANKI_DB_PORT=5434
> YANKI_WEB_PORT=8240`); docker via `sg docker -c`; prod loopback ports are
> 8142/8143 (health checks only — the shared Caddy reaches containers via
> network aliases `yanki-web`/`yanki-api` on `pulse-prod_default`);
> `deploy/*.sh` still never executed (debt #1); frontend lint =
> `eslint . --ext ... --max-warnings 0` (warnings FAIL CI; flat-config move
> rides the Next 16 bump, debt #16); gitleaks pinned in two places; the e2e
> CI job's install-before-boot order is load-bearing; Workflow `args` must
> be real JSON.
>
> **Acceptance:** chosen task's criteria in `docs/implementation-plan.md`
> met; statuses + debt updated; session log with next-session prompt in
> `docs/sessions/`.

---

## 7. Docs inventory audit

All 25 repo `.md` files checked (24 from session 5 + this log; brandkit's
five `.md` files are package components parked by operator decision, not
repo docs; dependency/build artifacts excluded): **all live, required, and
correctly placed**. **5 updated this session** (implementation-plan,
operator-expected, tech-debt, past-prompt, + this log); 0 moved, 0 deleted,
0 deprecated found.

## 8. Operator checklist refresh

[operator-expected.md](../operator-expected.md) refreshed. **Expected from
the operator right now, in priority order:**
1. **Item 1b — OpenAI billing/credits** (their key returns
   `insufficient_quota`) — unblocks the last P4.1 residual (~$0.02 re-run).
2. **Items 4/6/7 — the P4.2 supervised deploy**: real `POSTGRES_PASSWORD`
   in `deploy/.env`, the shared-Caddy edit in the ams-pulse repo
   (validate + reload), and supervising `make deploy`.
Items 8–10 (Phase-5 keys/decisions, brandkit v2) are parked, not due.
