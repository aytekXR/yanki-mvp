# Session 12 — 2026-07-10 (#10 today): P5.2 + P5.3 + P5.6 — checker backend complete, deployed dark

Operator prompt at start: continue implementation using workflows (starting
from `docs/resume-prompt.md`), log anything expected from the operator in
chat AND in `operator-expected.md`. Mid-session directives: report status +
readiness; close the session when workflows are done; prepare the next
session prompt; **never trigger workflows for docs-only changes** (now a
standing rule, consistent with the existing "workflows for code changes
only").

## 1. Session summary

Three Phase-5 tasks landed, each via an implement → 3-lens adversarial
verify → bounded-fix-loop workflow, then ONE deploy at close. Session-start
checks: prod healthy, CI 5/5 green through session 11, tree clean, prod DB
zero abuse (9 done MVP analyses only).

1. **P5.2 — checker pipeline branch** (commit `d6e7253`, ship, 0 blocking
   findings): `run_pipeline` branches on `kind='checker'` at exactly two
   points — step 1 seeds KYC from `Brand: {brand}. Category: {category}.`
   with **zero HTTP** (the `checker://` url is never fetched), step 3 uses
   the new fixed `checker-en-v1` 12-prompt set
   (`checker_prompts.generate(kyc, lang)`; unwired langs fall back to EN
   until P5.8; prompts never name the brand — the checker measures
   *unprompted* visibility). Steps 2/4–6, progress mapping, StepProgress
   contract untouched; NULL/'mvp' rows byte-identical. `_write_cache` is now
   an `ON CONFLICT DO NOTHING` upsert with a PG-gated concurrent-writer race
   test (**repays debt #6**); the `claim_next` checker skip-guard is removed
   and its test replaced with a claims-checker-rows assertion (**repays debt
   #19**). ADR-20. DRY_RUN checker run: 12 prompts × 4 mock engines = 48
   responses, deterministic geo_score 0.3333.
2. **P5.3 — presence map + competitors** (commit `c5e4f6d`, ship after one
   fix round): pure read-time `services/checker_summary.py` (ADR-21) —
   `engine_presence` from stored footprint booleans (sums pinned to
   total_responses/footprint_count), `competitors_appeared` via a
   deterministic proper-noun co-mention heuristic (adjacent-token grouping,
   EN/TR stoplist, case-insensitive brand+alias exclusion, once-per-answer
   counting, top-10). **The adversarial heuristic lens caught a real bug
   pre-merge**: possessive forms ("Nike's") escaped brand exclusion — fixed,
   plus recommendation-verb stoplist gaps. `ResultOut` gains the two
   nullable checker-only fields (`null` for MVP rows); contract regenerated
   additively, drift-free. 23-test suite.
3. **P5.6 — checker hardening** (commit `7542751`, ship after one fix
   round): fresh checker runs pass, in order, `CHECKER_ENABLED` kill-switch
   (default OFF → friendly parked 503), per-IP 10/hour 429, per-brand
   20/day 429 (normalized triple; cache hits don't count), rolling-24h $5
   cost cap (503 "at capacity"). Salted `ip_hash` (reuses P5.0's
   `IP_HASH_SALT` — the card's separate `rate_limit_salt` was aspirational,
   deliberately no second salt) into `checker_submissions`; rejected submits
   record NOTHING; $0 cache hits are exempt from all guards and still return
   their id for the email gate. The abuse-bypass lens probed XFF spoofing,
   counter bypass, malformed-XFF 500s, casefold bucket-splitting — and the
   fix round also **hardened the cost cap beyond the card**: it now projects
   in-flight (queued/running) fresh runs at `_EST_CHECKER_RUN_COST_USD`,
   closing an enqueue-backlog bypass. ADR-22; **repays debt #21**.
4. **Deploy** (last-good `7542751`): CI 5/5 green first, prod DB pre-flight
   (zero checker rows — nothing for the un-guarded worker to claim),
   `sg docker -c "make deploy"`, co-tenant container states byte-identical
   (diff of before/after snapshots), site + both loopback healths 200.
   Live behavioral proof at $0: fresh checker submit → **503 "the free
   checker is not open yet" with zero rows recorded**; MVP analysis GET
   unchanged with the new fields `null`; worker booted clean.
5. Suite at close: **146 backend + 31 frontend** (from 113 + 31 at start),
   ruff/mypy/tsc/eslint clean, gen-types drift-free, CI 5/5 green on all
   three commits (watched to completion on `7542751`).

**Session prod spend: $0.** (All acceptance was DRY_RUN/local; the live
checks were reads, one parked 503, and health probes.)

**Workflow-ops note (for future orchestrators):** the P5.3 implementer
completed all its work, then died emitting its final structured report —
five schema-parse failures on oversized fields (long strings with angle
brackets/log dumps). Recovery: verified the tree inline, replaced the
implement stage with a report-reconstruction agent, resumed the same run id
(nothing re-implemented). All later agents carry explicit STRUCTURED-OUTPUT
brevity rules (plain ASCII prose, counts not logs, ≤30-word list entries) —
carry these rules into future workflow briefs.

## 2. Documentation updates

- `docs/implementation-plan.md` — session-12 entry; ➡️ next = P5.4→P5.5 (or
  P5.7 if keys); readiness snapshot (Phase 5 **5/12**, plan 37/44 ≈ 84%,
  tests 146+31, sequencing-change note); P5.2/P5.3/P5.6 cards → done (each
  noting its card's stale debt-number artifact).
- `docs/tech-debt.md` — **#6, #19, #21 repaid** (with honest residuals on
  the cost cap, XFF, and $0 cache-hit row growth); new minor **#22**
  (cost-cap window/kind-scope not test-pinned).
- `docs/design.md` — **ADR-20** (pipeline branch + cache upsert), **ADR-21**
  (read-time summary heuristic), **ADR-22** (hardening + residuals) — all
  written in-workflow with the code.
- `docs/architecture.md` — checker-branch subsection under the pipeline
  diagram; P5.6 guard-chain table replacing the forward-looking one-liner.
- `deploy/.env.example` — the four new checker vars (`CHECKER_ENABLED=0`
  with the P5.11 note).
- `docs/operator-expected.md` — refreshed twice (interim on request + this
  close; see §8).
- `docs/past-prompt.md` — session-12 pointer. This log.

## 3. Architectural changes

Three ADRs, all additive (no schema migration this session): ADR-20
(checker rows reuse the six-step pipeline via a two-point `kind` branch;
`llm_cache` upsert makes multi-worker safe), ADR-21 (checker-only result
fields computed at read time from stored rows — no pipeline change, no LLM),
ADR-22 (four-guard chain on fresh checker runs, cache-hit exemption,
projected cost cap; accepted residuals documented). The public checker
surface exists on prod but is dark by config default.

## 4. Technical debt

- **Repaid:** #6 (cache race), #19 (claim_next guard), #21 (unthrottled
  checker endpoint).
- **Added:** #22 (minor: cost-cap window/kind-scope test gap — natural fix
  point is P5.7's `_EST_CHECKER_RUN_COST_USD` retune).
- #1 unchanged (KYC-cost persistence + adapter contract tests), #18
  unchanged (prod image devDeps).

## 5. Current state

- **Implemented:** MVP 32/32 LIVE; Phase 5 **5/12** — the checker
  **backend vertical is complete and deployed dark** (submit → hardened
  guards → pipeline → scored result with presence map + competitors, all
  parked behind `CHECKER_ENABLED=0`).
- **Incomplete:** P5.4/P5.5 (checker frontend + email gate), P5.7 (real
  Gemini/Perplexity — **operator keys are now the only blocker**),
  P5.8–P5.11.
- **Blockers:** none for P5.4/P5.5. P5.7 blocked on operator item 12;
  P5.11 on item 13.
- **Assumptions this session:** (a) "continue implementation" + the
  standing handoff authorized the one supervised deploy (proven `make
  deploy` path, co-tenant checks, kill-switch dark — and the mid-session
  operator message endorsed the plan that included it); (b) deploying the
  checker dark is the intended reading of the session-9 warning (the
  alternative — holding all three commits back — leaves prod unable to
  receive the frontend slices incrementally); (c) P5.6's defaults (10/h,
  20/brand/day, $5/day) are engineering guesses pending the operator's
  item-13 threshold decisions at P5.11.

## 6. Next-session prompt

> **You are the founder-orchestrator for Yanki** (`/home/aytek/repo/yanki-mvp`,
> brief: `docs/resume-prompt.md`). Read `docs/session-rules.md`,
> `docs/implementation-plan.md` (Current Priority + readiness snapshot),
> `docs/tech-debt.md`, `docs/operator-expected.md`, and this log first.
>
> **State:** MVP 32/32 LIVE at https://yanki.beyondkaira.com (rate-limited,
> $0.0162/analysis, $10 console caps). Phase 5 is **5/12**: P5.0/P5.1
> (session 9) + P5.2/P5.3/P5.6 (session 12) all deployed, CI 5/5 green,
> last-good `7542751`. The checker backend is COMPLETE and DARK: fresh
> submits 503 ("not open yet", records nothing) behind `CHECKER_ENABLED=0`;
> a DRY_RUN checker row runs all six steps with no crawl → 12 prompts × 4
> engines = 48 responses → score + `engine_presence` +
> `competitors_appeared` in the GET.
>
> **Priority: P5.4 — checker frontend (EN): landing + live results** (card
> in `docs/implementation-plan.md`): `/checker` route + `CheckerForm`
> (brand/category/lang), `createCheckerAnalysis()` → `/checker/[id]?submission_id=…`,
> poll existing `getAnalysis`, reuse `StepProgress`/`ScoreGauge`, new
> `EnginePresenceMap` + `CompetitorsList`, `lib/i18n.ts` typed dict (EN now,
> `tr` placeholder), brandkit §2 tokens + §7 a11y, axe smoke both routes.
> Then **P5.5 — email gate** (one free answer, gate the rest, POST
> `{submission_id, email}` to `/api/v1/checker/leads`). Both $0/DRY_RUN.
> **If the operator has supplied `GEMINI_API_KEY` + `PERPLEXITY_API_KEY`
> (operator-expected item 12), P5.7 is unblocked and may take priority** —
> respx-tested only, never live in CI.
> Dev note for P5.4 acceptance: to exercise a checker submit end-to-end
> locally you must set `CHECKER_ENABLED=1` in the dev env (default is off
> everywhere; dev compose reads `deploy/.env`) — the parked 503 is the
> correct default behavior, not a bug.
>
> **Constraints:** repo always runnable (`make dev`/`make test`); workflows
> (implement → adversarial-verify) for code changes, **never docs-only
> changes** (operator directive, session 12); include the session-12
> STRUCTURED-OUTPUT brevity rules in every workflow agent brief (see
> sessions/2026-07-10-10.md §1 workflow-ops note); never hand-edit
> generated files (`make gen-types`); docs in the same session as code;
> scope frozen per `docs/02-mvp.md` §4 + Phase-5 cards; eight session-end
> deliverables per `docs/session-rules.md`; Workflow `args` must be real
> JSON.
>
> **Warnings:** PROD IS LIVE ON THIS BOX — project `yanki-prod`, loopback
> health 8142/8143 (`/healthz`), attached to `pulse-prod_default` (never
> take pulse-prod down while attached; never restart the shared Caddy;
> never append the yanki Caddy block twice — debt #7). Deploys via
> `sg docker -c "make deploy"` after CI green; snapshot co-tenant container
> states before/after. Do NOT flip `CHECKER_ENABLED` on prod — that is the
> operator's P5.11 go-live step. A live MVP analysis costs ~$0.016–0.02;
> P5.4/P5.5 acceptance is fully DRY_RUN/$0. Dev ports: 5432/8140 taken
> (`YANKI_DB_PORT=5434 YANKI_WEB_PORT=8240`); Node 20 vs README's 22 (fine);
> `sg docker -c` for docker; worker logs one cosmetic UndefinedTable at
> first boot (debt #16); analysis terminal status is `done`; `geo_score`
> nested under `result`.
>
> **Acceptance:** P5.4's card criteria (DRY_RUN stack: submit → progress →
> score + presence map + competitors + raw answers; client-side invalid-brand
> rejection; axe smoke; `npm test -- --run` green) + P5.5's if room (gate
> hides all but one answer; valid email reveals + persists per-submission;
> invalid email inline error; keyboard-operable) + `make test` green + CI
> green; statuses + debt updated; session log with next-session prompt;
> operator checklist refreshed.

## 7. Docs inventory audit

36 repo `.md` files enumerated (dependency/build artifacts excluded):
**30 audited** (4 root/.github community files + 15 under `docs/` + 11
session logs incl. this one) — **all live, required, correctly placed** —
plus **6** under `brandkit/` that remain parked package components by
operator decision (count correction: earlier logs said "five"; there are
six — `uploads/` carries three snapshot copies of our docs, which are part
of the parked package, not live docs). 7 updated this session
(implementation-plan, tech-debt, design, architecture, operator-expected,
past-prompt, + this log); 0 moved, 0 deleted, 0 deprecated found.

## 8. Operator checklist refresh

[operator-expected.md](../operator-expected.md) refreshed twice (interim
mid-session on request, full at close). **Expected from the operator right
now: nothing blocking; ONE recommended item** — item 12, the
`GEMINI_API_KEY` + `PERPLEXITY_API_KEY` pair (+ grounding/ToS sanity
check), now the only thing between us and P5.7. No urgency: next session
(P5.4/P5.5) proceeds at $0 without them. Standing: do NOT flip
`CHECKER_ENABLED` (P5.11, after item-13 decisions); item 13 decisions come
due at P5.11; item 14 (brandkit) stays parked at the operator's word.

---

## 9. Post-close addendum (same day) — operator re-scope: EN-only + brandkit v2

After the close above, the operator directed (verbatim intent): **skip
Turkish — whole product English**; **stage Gemini/Perplexity key fields in
`.env` (they will paste keys)**; **dive into the UI refactor with the given
brandkit**; **refactor the plan**. All executed as docs/config-only changes
(no workflows, per standing rule):

- **P5.8/P5.9 SKIPPED** (cards preserved, marked, with revival notes);
  P5.10/P5.11/P5.4 cards amended EN-only (P5.11's Turkish sign-off gate
  void — EN-only is now the authorized plan, not a deviation); roadmap 2c
  moved to Later (+ wedge annotation, out-of-scope mapping row, Arabic-gate
  suspension note).
- **New P5.12 — brandkit v2 UI refactor** (echo identity: teal petrol
  tokens, Sora + IBM Plex Mono, v2 score bands where 30–59% is `warning`),
  jumps AHEAD of P5.4 so the checker UI is built once. v2 supersedes
  frontend-brandkit v1 (operator item 14: decided). Includes the manual
  WCAG ratio recheck (debt #13) and before/after screenshots.
- **Key fields staged**: `GEMINI_API_KEY=` / `PERPLEXITY_API_KEY=` appended
  to gitignored `deploy/.env` (operator pastes values; inert until P5.7)
  and documented in `deploy/.env.example`.
- **Counts**: Phase 5 is now 11 tasks (12 − 2 skipped + 1 added), 5/11
  done; enlarged plan **37/43 ≈ 86%**. Sessions to go-live now estimated
  **3**: (13) P5.12 [+P5.7 if keys present], (14) P5.4+P5.5, (15)
  P5.10+P5.11.
- operator-expected.md updated (item 12 → paste-here instructions, item 13
  Turkish decider withdrawn, item 14 decided/scheduled).

### Superseding next-session prompt (replaces §6 above)

> **You are the founder-orchestrator for Yanki** (`/home/aytek/repo/yanki-mvp`,
> brief: `docs/resume-prompt.md`). Read `docs/session-rules.md`,
> `docs/implementation-plan.md` (Current Priority + readiness snapshot +
> the Phase-5 scope-change note), `docs/tech-debt.md`,
> `docs/operator-expected.md`, and this log (§9 supersedes §6) first.
>
> **State:** MVP 32/32 LIVE at https://yanki.beyondkaira.com (rate-limited,
> $0.0162/analysis, $10 console caps). Phase 5 is **5/11** (37/43 ≈ 86%)
> after the operator re-scope: **English-only product** (P5.8/P5.9 skipped),
> brandkit v2 adoption added as **P5.12**. Checker backend complete and
> DARK (`CHECKER_ENABLED=0`; fresh submit → 503, records nothing; last-good
> `7542751`, CI 5/5 green).
>
> **Priority: P5.12 — brandkit v2 UI refactor** (card in
> implementation-plan.md): port `brandkit/brandkit/frontend-brandkit-v2.md`
> §2 tokens into `frontend/tailwind.config.ts`, Sora + IBM Plex Mono via
> `next/font` (self-hosted, no CDN), v2 score bands in ScoreGauge (30–59% =
> `warning` now), restyle ALL existing surfaces token-for-token (no
> behavior change), reconcile `docs/frontend-brandkit.md` to v2, ADR,
> manual WCAG ratios recorded (debt #13), before/after screenshots in the
> session log. **Check `deploy/.env` for `GEMINI_API_KEY`/`PERPLEXITY_API_KEY`
> values first (grep for non-empty, do NOT print them): if the operator has
> pasted keys, P5.7 (real Gemini + Perplexity adapters, respx-only in CI,
> price-table costs) can run as an independent backend lane in the same
> session.** Then P5.4 (checker frontend, EN-only, no i18n machinery, on
> the v2 tokens) → P5.5 if room.
>
> **Constraints:** repo always runnable; workflows (implement →
> adversarial-verify) for code changes, never docs-only (operator
> directive); include the session-12 STRUCTURED-OUTPUT brevity rules in
> every workflow brief (§1 workflow-ops note); never hand-edit generated
> files; docs same-session as code; scope per the amended Phase-5 cards —
> Turkish stays out unless the operator revives it; eight session-end
> deliverables; Workflow `args` must be real JSON.
>
> **Warnings:** PROD LIVE ON THIS BOX (`yanki-prod`, loopback 8142/8143,
> `/healthz`, shared Caddy — never restart it; co-tenant snapshot
> before/after deploys; deploy via `sg docker -c "make deploy"` after CI
> green). Do NOT flip `CHECKER_ENABLED` on prod (operator's P5.11 step).
> P5.12 must not break the CI browser e2e (restyle, not restructure — keep
> selectors/roles stable). Frontend contrast: axe can't check it under
> jsdom — compute the v2 ratios by hand and record them. A live MVP
> analysis costs ~$0.016–0.02; P5.12/P5.4/P5.5 are $0. Dev ports: 5432/8140
> taken (`YANKI_DB_PORT=5434 YANKI_WEB_PORT=8240`); `sg docker -c` for
> docker; terminal status `done`; `geo_score` under `result`.
>
> **Acceptance:** P5.12's card criteria (v2 palette/type everywhere, zero
> raw hexes, v2 score bands + numeric label, axe suites green, tsc/eslint/
> build green, e2e green, screenshots attached) + `make test` + CI green
> (+P5.7's card criteria if keys were present); statuses + debt updated;
> session log with next-session prompt; operator checklist refreshed.

