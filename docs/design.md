# Yanki — Design & Architecture Decisions

*Audience: the whole team. This is the map of **where code lives**, **who owns
what**, and **why the big calls were made**. For product scope read
[02-mvp.md](02-mvp.md); for the FE/BE contract mechanics read the master SPEC;
for runtime/deploy topology read [architecture.md](architecture.md).*

This doc has four parts:

1. [Repo structure](#1-repo-structure--per-folder-rationale) — the tree and why each folder exists.
2. [Ownership map](#2-ownership-map) — who may edit what, and the review rules.
3. [Coding conventions](#3-coding-conventions) — the house style everyone follows.
4. [ADR log](#4-adr-log) — the numbered record of decisions.

---

## 1. Repo structure & per-folder rationale

This expands the README's "repo mini-map". The rule of thumb — *a file's owner
is the owner of the directory it lives in* — is what makes this layout safe for
several people (and several agents) to build in parallel.

```
yanki/
├── backend/                # Python 3.12 — FastAPI api + worker + GEO engine (one image)
│   ├── pyproject.toml       # backend deps + tool config (uv, ruff, mypy, pytest)
│   ├── Dockerfile           # python:3.12-slim + uv sync; the SAME image runs api and worker
│   ├── alembic/             # DB migrations — one initial migration creates all four tables
│   └── app/
│       ├── __init__.py
│       ├── config.py        # pydantic-settings: every env var + its default lives here
│       ├── worker.py        # the queue poller: claim a job, run the 6 pipeline steps
│       ├── api/             # HTTP layer: FastAPI routes + Pydantic request/response schemas
│       ├── services/        # orchestration glue between api ⇄ db ⇄ queue (create/get an analysis)
│       ├── db/              # SQLAlchemy 2.0 models, Base, and the session factory
│       ├── jobs/            # the Postgres job queue (FOR UPDATE SKIP LOCKED claim + stale reaper)
│       ├── pipeline/        # the GEO engine: discovery → kyc → prompts → execute → footprint → scoring
│       ├── providers/       # LLM adapters behind one Provider interface (+ deterministic mock)
│       └── tests/           # pytest: api + queue tests, plus pipeline/ subtree owned by the pipeline agent
├── frontend/               # Next.js 15 (App Router) + TypeScript + Tailwind — 3 screens
│   ├── app/                 # routes: / (submit) and /analyses/[id] (progress → results / error)
│   ├── components/          # Button, UrlForm, StepProgress, ScoreGauge, ResultsTable
│   ├── lib/                 # api.ts (fetch wrapper) + types.ts (GENERATED from openapi.json)
│   └── e2e/                 # Playwright happy-path (skipped unless E2E_BASE_URL is set)
├── shared/                 # cross-language contract surface
│   └── contracts/openapi.json   # GENERATED by `make gen-types`; single source of the API shape
├── deploy/                 # Docker Compose + deploy/rollback scripts (ams-pulse pattern) + .env.example
├── scripts/                # repo-level dev utilities (bootstrap, gen-openapi, check-env)
├── .github/                # CI/CD workflows, PR/issue templates, CODEOWNERS
└── docs/                   # design, architecture, MVP scope, roadmap, brandkit, tests (one file per topic)
```

**Why the split the way it is:**

- **`backend/app/api` vs `services` vs `db` vs `jobs`.** The API layer only
  speaks HTTP; it never touches SQLAlchemy directly. `services/` is the thin
  seam that turns a request into a DB row and a queued job, so the pipeline and
  the API never import each other. `db/` owns the schema; `jobs/` owns the one
  tricky SQL statement (the `SKIP LOCKED` claim). Keeping the claim in its own
  module means the "clever" concurrency code is one small, reviewable file.
- **`pipeline/` is one module per step.** discovery, kyc, prompts, execute,
  footprint, scoring — each is a plain sync function with a clear signature.
  A junior can read any single step end-to-end without holding the rest of the
  system in their head, and each step is unit-testable in isolation.
- **`providers/` behind one interface.** Every model (Claude, OpenAI, the two
  stubs, and the mock) implements the same `Provider` protocol, so the pipeline
  never branches on "which engine". Swapping a stub for a real engine is a
  one-file change and touches nothing else.
- **`shared/contracts/`** exists so the frontend and backend agree on the API
  shape via a checked-in artifact, not by reading each other's code.
- **`deploy/`, `scripts/`, `.github/`** are the operational surface, kept out of
  the app so infra changes never collide with feature work.
- **`docs/` is one file per topic** so parallel doc work never conflicts.

---

## 2. Ownership map

### The ownership rule

**A file's owner is the owner of the directory it lives in.** You edit only
files inside directories you own. Four paths are extra-sensitive and, on top of
their normal owner, **also require the lead's review** on every change:

- `deploy/**` — production topology and secrets handling.
- `.github/**` — CI/CD; a bad workflow blocks everyone.
- `shared/contracts/**` — the FE/BE contract; drift here breaks both sides.
- `backend/alembic/**` — migrations run against real data and can't be un-run.

### Who owns what

| Path | Owner |
| --- | --- |
| `backend/` (spine: `pyproject.toml`, `Dockerfile`, `alembic/`, `app/{config,worker}.py`, `app/api/**`, `app/db/**`, `app/jobs/**`, `app/services/**`, `tests/{conftest,test_api,test_queue}.py`) | backend-spine agent |
| `backend/app/pipeline/**`, `backend/app/providers/**`, `backend/tests/pipeline/**` | pipeline agent |
| `frontend/**` | frontend agent |
| `shared/contracts/openapi.json` | generated — no owner hand-edits (see below); **+ lead review** |
| `Makefile`, `deploy/**`, `scripts/**`, `.github/**`, `.gitignore`, `CONTRIBUTING.md`, `SECURITY.md` (+ README link fixes) | infra agent |
| `docs/**` | doc agents (one file each) |

`backend/pyproject.toml` is owned by the backend-spine agent; the pipeline agent
may **not** edit it even though pipeline code imports its deps. Need a new dep?
Ask the spine owner.

### Generated files — never hand-edit

Two files are produced by `make gen-types` and are **checked in** so CI can diff
them and fail on drift. Editing them by hand is always wrong — regenerate
instead:

- `shared/contracts/openapi.json` — exported from `app.openapi()`.
- `frontend/lib/types.ts` — produced from that JSON by `openapi-typescript`.

The flow is: change a Pydantic schema → `make gen-types` → commit the two
regenerated files alongside your code. CI runs `make gen-types` and fails if the
result differs from what's committed (NFR-6, contract safety).

---

## 3. Coding conventions

**Backend (Python):**

- **Sync Python only.** No `async`/`await` anywhere in the pipeline or worker.
  FastAPI route handlers are plain `def`. (Rationale: ADR-1.)
- **SQLAlchemy 2.0 style** — typed `Mapped[...]` models, `select()` queries, and
  the `Session` context manager from `app/db/session.py`. No legacy Query API.
- **One env var, one place.** Every setting is declared in `app/config.py`
  (pydantic-settings) with a default. Code reads settings; it never reads
  `os.environ` directly.
- **Pipeline steps are pure-ish functions** with explicit inputs/outputs;
  scoring and footprint are *pure* (no I/O) so they're trivially testable.
- **ruff** formats and lints; **mypy** type-checks. Both run in `make lint` /
  `make typecheck` and in CI.
- **Errors that should fail a job** raise `PipelineError`; the worker turns any
  exception into `status='failed'` + a stored `error`, keeping partial rows.

**Frontend (TypeScript / Next.js):**

- **Tailwind design tokens, never raw hex.** Colors, spacing, radii, and type
  come from the tokens in [frontend-brandkit.md](frontend-brandkit.md) §2, copied
  into `tailwind.config`. Write `bg-brand`, not `bg-[#123456]`. This keeps the UI
  consistent and re-themeable.
- **Fetch relative paths only** (`/api/v1/...`, `/healthz`). Next.js `rewrites()`
  proxy them to the API in dev; Caddy path-routes them in prod. Never hard-code
  the API origin in a component. (Rationale: ADR-6.)
- **Types come from the contract.** Import request/response types from the
  generated `lib/types.ts`; don't re-declare API shapes by hand.

---

## 4. ADR log

Short records of the decisions that shape the codebase. Format: **context →
decision → consequences**, with one line on why the alternative was rejected.

### ADR-1 — Sync Python throughout
- **Context:** the pipeline is I/O-bound (HTTP crawls, LLM calls) but strictly
  sequential per job, and juniors must be able to read it.
- **Decision:** all backend code is synchronous — no asyncio in the pipeline or
  worker; FastAPI handlers are plain `def`.
- **Consequences:** simpler stack traces, no event-loop foot-guns, easy testing.
- **Rejected:** async/await — buys nothing for one-job-at-a-time work and adds
  cognitive load and colored-function complexity (violates NFR-4).

### ADR-2 — Postgres as the job queue (no broker)
- **Context:** we need a durable background queue with at-least-once delivery.
- **Decision:** the `analyses` table *is* the queue. The worker claims a row in
  one transaction with `... FOR UPDATE SKIP LOCKED LIMIT 1`, sets it `running`,
  and a stale-claim reaper re-queues rows whose `claimed_at` aged out.
- **Consequences:** one fewer moving part; the queue is transactional with the
  data; a crash never loses or double-runs a job (NFR-3).
- **Rejected:** Redis/RabbitMQ/Celery — another service to run, monitor, and
  secure, for a workload Postgres handles at our scale.

### ADR-3 — One Docker image runs api + worker
- **Context:** api and worker share all the same code and dependencies.
- **Decision:** build a single image; run it twice with different commands
  (`uvicorn app.api.main:app` vs the worker loop).
- **Consequences:** one Dockerfile, one build, guaranteed code parity between the
  two processes; scale each independently by replica count.
- **Rejected:** two images — duplicate builds and a real risk of api/worker
  drifting out of sync.

### ADR-4 — `uv` for Python dependency management
- **Context:** need fast, reproducible installs in dev, CI, and the image.
- **Decision:** manage backend deps with `uv` and a locked `pyproject.toml`;
  `uv sync` in the Dockerfile and `make setup`.
- **Consequences:** fast, deterministic installs; a single lockfile everyone shares.
- **Rejected:** pip + requirements.txt / Poetry — slower and/or heavier for no
  benefit here.

### ADR-5 — Makefile as the single control panel
- **Context:** contributors need one obvious place for every routine task.
- **Decision:** every workflow (setup, dev, test, lint, fmt, typecheck, migrate,
  gen-types, deploy, rollback) is a `make` target; `make help` is the default.
- **Consequences:** discoverable commands, identical invocations locally and in
  CI, no tribal knowledge.
- **Rejected:** scattered per-tool commands / a task runner like `just` — one
  more tool to install; `make` is already everywhere.

### ADR-6 — Next.js `rewrites()` instead of CORS
- **Context:** in dev the web (8140) and api (8141) are different origins.
- **Decision:** Next.js `rewrites()` proxy `/api/:path*` and `/healthz` to
  `API_ORIGIN` (default `http://localhost:8141`); the frontend always fetches
  relative paths. In prod, Caddy path-routes on one origin.
- **Consequences:** no CORS config anywhere; dev and prod use identical relative
  URLs in the frontend.
- **Rejected:** enabling CORS on the API — extra config, wildcard/security
  headaches, and a dev/prod behavior mismatch.

### ADR-7 — DRY_RUN-first defaults (deterministic mock, $0 CI)
- **Context:** real LLM calls cost money and are non-deterministic; CI and
  first-run must be free and repeatable.
- **Decision:** `DRY_RUN` defaults to **true**. In DRY_RUN the whole pipeline
  runs on `MockProvider` — deterministic output, `cost_usd = 0`, no API keys.
- **Consequences:** CI costs $0 (NFR-1), tests are deterministic, and a new
  contributor gets a full working run with zero setup.
- **Rejected:** always calling real providers (needs keys, costs money, flaky
  tests) or a separate fake-only test harness (duplicates the real code path).

### ADR-8 — Deterministic template-based prompt generation
- **Context:** the pipeline needs N prompts derived from the KYC profile.
- **Decision:** generate prompts from **deterministic templates** that cycle a
  fixed set of categories (recommendation, comparison, alternatives, best-of,
  use-case) and fill slots from KYC fields. No LLM in this step.
- **Consequences:** prompts are free, fast, reproducible, and unit-testable
  (exactly `PROMPT_COUNT`, all non-empty, no duplicates).
- **Rejected:** LLM-generated prompts — costs money, is non-deterministic (hard
  to test), and adds a failure mode for no MVP benefit. It is on the roadmap.

### ADR-9 — Gemini & Perplexity stubbed behind the Provider interface
- **Context:** the panel should represent four engines, but only Claude and
  OpenAI are wired to real SDKs for the MVP.
- **Decision:** Gemini and Perplexity are **stub** providers that implement the
  same `Provider` protocol and return a canned plausible answer (model `"stub"`,
  cost 0) — sometimes mentioning nothing, to exercise the footprint logic.
- **Consequences:** the panel is four-wide today; making a stub real is a
  single-file swap with zero changes to the pipeline.
- **Rejected:** shipping only two engines (misrepresents the product surface) or
  wiring all four SDKs now (more keys, cost, and integration risk than the MVP
  needs). Real Gemini/Perplexity are explicitly out of scope (02-mvp §4).

### ADR-10 — `llm_cache` table with a 24h TTL
- **Context:** re-running the same prompt against the same model within a job (or
  across quick reruns) shouldn't pay twice.
- **Decision:** an `llm_cache` table keyed by `sha256(engine:model:prompt_text)`;
  reads use rows fresher than 24h, and stale rows are ignored and overwritten.
- **Consequences:** cheaper reruns and a simple cost control (FR-8) with no
  cross-account leakage — the MVP caches only within a job's own runs.
- **Rejected:** no cache (pays repeatedly) or a shared cross-account cache
  (privacy + correctness concerns) — both out of scope for the MVP.

### ADR-11 — GEO scoring is a pure function in `pipeline/scoring.py`
- **Context:** the GEO score is the product's headline number and must be
  transparent and defensible ("show your work").
- **Decision:** scoring lives in `pipeline/scoring.py` as a **pure function**
  `geo_score(footprints, total) -> float = footprints / total`, returning `0.0`
  when `total == 0` (no divide-by-zero).
- **Consequences:** trivially unit-testable, deterministic, and easy to explain;
  the weighted (mention × position × sentiment) version can replace it later
  behind the same signature.
- **Rejected:** computing the score inline in the worker or the API — would mix
  I/O with the formula, making it hard to test and easy to drift. Referenced by
  number in 02-mvp §5.

### ADR-12 — Strict URL validation (reject schemeless input)
- **Context:** the submit endpoint takes an arbitrary user-typed URL.
- **Decision:** accept `http`/`https` only and **reject** anything without a
  scheme (return `422`); we do **not** silently prepend `https://`.
- **Consequences:** validation stays strict and simple, errors are explicit, and
  discovery never gets a malformed target.
- **Rejected:** auto-normalizing a schemeless string — hides user mistakes and
  invites ambiguous/unsafe inputs for a marginal convenience.

### ADR-13 — SQLAlchemy 2.0 sync + Alembic, one initial migration
- **Context:** we need typed models and versioned schema changes.
- **Decision:** SQLAlchemy 2.0 (`Mapped[...]`, `select()`) in sync mode, with
  Alembic; the MVP ships **one** initial migration creating all four tables
  (`analyses`, `prompts`, `responses`, `llm_cache`).
- **Consequences:** typed, modern ORM usage consistent with ADR-1; schema history
  starts clean and reviewable.
- **Rejected:** raw SQL / `create_all()` at boot — no migration history, harder
  to evolve safely against real data.
</content>
